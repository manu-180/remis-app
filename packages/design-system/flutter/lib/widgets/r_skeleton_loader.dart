import 'package:flutter/material.dart';

/// RSkeletonLoader — shimmer placeholder for loading states. TODO: implement full widget.
class RSkeletonLoader extends StatelessWidget {
  const RSkeletonLoader({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
