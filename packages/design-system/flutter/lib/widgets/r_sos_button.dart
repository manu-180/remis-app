import 'package:flutter/material.dart';

/// RSosButton — emergency SOS trigger button. TODO: implement full widget.
class RSosButton extends StatelessWidget {
  const RSosButton({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
