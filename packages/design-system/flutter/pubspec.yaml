name: remis_design_system
description: Design tokens and theme for the remis app (stub — Tanda 1B will expand)
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0
  flutter: ^3.27.0

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
